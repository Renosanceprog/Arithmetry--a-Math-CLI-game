//
#include <stdio.h>
#include <conio.h>

int main(){

    // char a, b, c;
    // a = getch();
    // b = getch();
    // c = getch();
    // printf("%c %c %c", a, b, c);
    printf("%d", '\r');
    return 0;
}